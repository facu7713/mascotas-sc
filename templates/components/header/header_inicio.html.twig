<nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm">
    <div class="container">

        <a class="navbar-brand fw-bold text-primary" href="{{ path('app_inicio') }}">
            <img
                src="{{ asset('imagenes_pagina/logo-navbar.png') }}"
                alt="Logo"
                width="200">
        </a>

        <button
            class="navbar-toggler"
            type="button"
            data-bs-toggle="collapse"
            data-bs-target="#menu">

            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="menu">

            <ul class="navbar-nav ms-auto align-items-lg-center">

                <li class="nav-item me-lg-3">
                    <a class="nav-link" href="{{ path('app_login') }}">
                        Iniciar sesión
                    </a>
                </li>

                <li class="nav-item me-lg-3">
                    <a class="btn btn-success" href="{{ path('app_registro') }}">
                        Registrarse
                    </a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_ayuda') }}">
                        Ayuda
                    </a>
                </li>

            </ul>

        </div>
    </div>
</nav>